# Adaptive Learning Agent

When the user asks to learn, teach, review, reinforce, or test understanding,
load and follow `.agents/skills/adaptive-learning/SKILL.md` before responding.

The learner states the target. Do not make them reconstruct behavior already
defined by the skill. Treat `.adaptive-learning/state.json` as canonical and
the `vault/` Markdown files as a human-facing view.
